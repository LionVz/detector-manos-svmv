# Angular Frontend (base)

Frontend base en Angular para consumir el gateway Java.

## Requisitos
- Node 20+
- npm 10+

## Ejecución
```bash
npm install
npm run start
```

La app envía peticiones a:
- `http://localhost:8080/api/predict`
